<UserControl x:Class="TheDebtBook.Views.DebtorsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TheDebtBook.Views"
             mc:Ignorable="d" 
             Background="{StaticResource BackgroundBrush}"
             d:DesignHeight="200" d:DesignWidth="300">

    <Grid >
        <StackPanel>
            <TextBlock Text="Details of debt:" HorizontalAlignment="Center"/>
            <ToolBar HorizontalAlignment="Center">
                <Button Content="Close window"/>
                <Button Content="Add post"
                        Command="{Binding Path=NewPost}"
                />
                <Button Content="Remove post"/>
            </ToolBar>

            <DataGrid 
                MinHeight="150" 
                Name="ListOfDetails"
                ItemsSource="{Binding Path= CurrentDebitor.DebitorCreditorDetails }"
                SelectedItem="{Binding Path= CurrentPost}"
                SelectedIndex="{Binding Path=CurrentIndex}"
                IsSynchronizedWithCurrentItem="True"
                CanUserAddRows="True"
                CanUserDeleteRows="True"
                IsReadOnly="False"
                >

                
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>
